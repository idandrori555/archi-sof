# Final Project - Archi

I didn't really know what to submit so I just submitted the DLL at DLL/ (VS project)
and the injector files at project/ (Makefile provided)

PS:
You can either compile the DLL && the injector EXE with the makefile, OR compile the DLL seperately, put it in the folder with the EXE and run it.

PS 2.0:
The DLL's Name has to be "mydll.dll" (If you chose to build from VS)
